#
# ZSH Shell Parameters
#
# Note: Lower case shell parameters are usually arrays.
#       UPPER CASE shell paramaters are usually colon-separated lists.

# Report about the time a command took if it runs longer than 5 seconds
# Format: cpu-time / system-time / user-time
REPORTTIME=5

# Notify me of login events for everyone except me and root
watch=(notme root)